<%= render 'shared/error_messages', object: @cohort if @cohort.errors.any? %>


<div class="container">

  <label class="label">Name</label>
  <p class="control">
    <%= form.input :name, placeholder: "Class of 2016" %>
  </p>

  <label class="label">Year</label>
  <p class="control">
    <%= form.input :year, collection: Cohort::CLASS_YEARS, :class => "select" %>
  </p>

</div>

  <p class="control">
    <%= form.submit "Submit", :class => "button is-info" %>
  </p>
