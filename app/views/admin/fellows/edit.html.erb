<% title "Edit: #{@fellow.name}" %>
<h3>Edit: <%= @fellow.name %> </h3>

<%= form_for [:admin,@cohort, @fellow],:url => admin_fellow_path(@cohort, @fellow), :html => { :multipart => true } do |f| -%>

  <%= render f %>

  <%= f.submit "Submit", :class => "button" %>
<% end -%>

<% title "Admin - #{@fellow.first_name}" %>

<div class="section">
  <div class="container">
    <div class="title">Edit: <%= @cohort.name %> Program #<%= @cohort.id %></div>
    <div class="container">
      <%= form_for [:admin, @cohort] do |f| %>
        <%= render f %>
      <% end %>
    </div>
  </div>
</div>
